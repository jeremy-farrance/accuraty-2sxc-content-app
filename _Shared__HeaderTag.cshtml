@{
  var title = PageData["title"] ?? Content.Title;
}

@if (Presentation.HeadingType != "hide") {
  @Html.Raw("<" + Presentation.HeadingType + ">")
    @title
  @Html.Raw("</" + Presentation.HeadingType + ">")
}
